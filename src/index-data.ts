/**
 * The docs index the server serves. Generated at build time from the sibling docs (see
 * scripts/build-index.mjs → src/generated/index.ts, which is gitignored). Run `pnpm build:index`
 * before typechecking/running if src/generated/index.ts is missing.
 */
export { INDEX } from './generated/index.js';
